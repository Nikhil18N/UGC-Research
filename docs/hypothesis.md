# Hypothesis

H1: A lightweight ML classifier trained on tuple and column access features will achieve higher F1 score than a fixed-threshold hybrid heuristic baseline.

H2: The ML model can remain low-overhead enough for practical periodic tiering decisions in in-memory environments.
