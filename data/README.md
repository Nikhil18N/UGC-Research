# Data Folder

- `raw/`: synthetic access logs generated by the simulator.
- `processed/`: engineered features and labels for ML training.

This project uses synthetic data by default to keep the study lightweight and reproducible.
